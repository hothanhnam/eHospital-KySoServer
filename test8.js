const fs = require('fs'); const html = fs.readFileSync('public/index.html', 'utf8'); let lines = html.split('\n'); let stack = []; for(let i=365; i<=380; i++) { let line = lines[i]; let opens = [...line.matchAll(/<div\b[^>]*>/gi)]; let closes = [...line.matchAll(/<\/div>/gi)]; for(let m of opens) stack.push({line: i+1, tag: m[0]}); for(let m of closes) { let pop = stack.pop(); console.log('Line ' + (i+1) + ' closes ' + pop.line); } console.log((i+1) + ': ' + line); }
