const fs = require('fs'); const html = fs.readFileSync('public/index.html', 'utf8'); let lines = html.split('\n'); let stack = []; for(let i=0; i<lines.length; i++) { let line = lines[i]; let opens = [...line.matchAll(/<div\b[^>]*>/gi)]; let closes = [...line.matchAll(/<\/div>/gi)]; for(let m of opens) stack.push({line: i+1, tag: m[0]}); for(let m of closes) stack.pop(); if (lines[i].includes('tab-benh-nhan')) break; } console.log(stack.map(s => s.line + ': ' + s.tag).join('\n'));
