const fs = require('fs'); const html = fs.readFileSync('public/index.html', 'utf8'); let divCount = 0; let lines = html.split('\n'); for (let i = 0; i < lines.length; i++) { const opens = (lines[i].match(/<div\b[^>]*>/gi) || []).length; const closes = (lines[i].match(/<\/div>/gi) || []).length; divCount += opens - closes; if (lines[i].includes('tab-nhan-vien')) console.log('Line ' + (i+1) + ': ' + lines[i].trim() + ' | divCount=' + divCount); } console.log('Final div count: ' + divCount);
