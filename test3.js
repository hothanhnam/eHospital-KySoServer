const fs = require('fs'); const html = fs.readFileSync('public/index.html', 'utf8'); const start = html.indexOf('<div id="tab-nhan-vien"'); const end = html.indexOf('<!-- End tab-nhan-vien -->'); const sec = html.substring(start, end); let count = 0; let lines = sec.split('\n'); for(let i=0; i<lines.length; i++) { count += (lines[i].match(/<div\b[^>]*>/gi)||[]).length - (lines[i].match(/<\/div>/gi)||[]).length; if (count <= 0) { console.log('Closed at relative line: ' + i + '\n' + lines[i]); break; } }
